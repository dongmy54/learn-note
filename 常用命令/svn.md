## svn
```shell
# 拉取代码
svn checkout <repo_url> [<local_dir>]
# 比如：svn co svn://example.com/repo

svn add newfile.txt
svn commit -m "<commit_message>"

# 上传更新
svn up

svn checkout https://192.168.1.167/svn/成都研发中心/周报/董名晏 --username mingyan.dong --password 20241101

```
