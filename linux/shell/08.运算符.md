#### 运算符

```bash
+    # 加
-    # 减
*    # 乘
/    # 除
**   # 乘方
%    # 求余

#========== 计算值
# expr 方式
# 1. 它是命令
# 2. 仅支持整数
expr 10 + 20
a=`expr 10 + 20`
echo $a
# => 30

# let、(())方式
# (()) 很常用，是let的简写
let a=10+20
((a= 10 + 20))    # 与上等价
echo $a
# => 30
```
