#### ls列出目录下文件

##### 用法
```
ls            当前路径下文件
ls /root /    /root 和 /下文件

ls -l         长格式显示(文件大小、所属人、分组、权限等)
ls -a         所有文件包含隐藏文件
ls -l -r      按文件名倒叙显示（z ->a）
ls -l -r -t   按文件更新时间递增排列
ls -lrt       同上 多个选项可合并
ls -R         递归列出文件
ls -lh temp   以利于阅读的方式显示文件大小
```

